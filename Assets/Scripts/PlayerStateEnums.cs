using System;

public enum ObstacleColorType
{
    Black,
    Red,
    Green,
    White,
    Count
}

public enum PlayerColorType
{
    Red,
    Green
}

public enum PlayerMovementType
{
    Idle,
    Jumping
}